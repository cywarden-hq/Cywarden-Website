import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Cywarden | Cookie Policy",
  description:
    "How Cywarden uses cookies and similar technologies on cywarden.com, and how you can manage your preferences.",
};

export default function CookiePolicyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
